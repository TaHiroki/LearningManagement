.row.justify-content-center
  .submain.col-lg-11
    h1.ml-5 科目編集

    - @subjects.each do |subject|
      .subject.shadow-lg.p-3.m-5.bg-white.rounded
        = "科目：#{subject.subject}　　マス数：#{subject.count}個"
        .subject.d-flex.justify-content-end
          = link_to '修正', edit_subject_path(subject), class: 'btn btn-primary'
          = link_to '削除', subject_path(subject), method: :delete,
            data: { confirm: "科目を削除しますか？"}, class: 'btn btn-danger' 